/**
 * 数读生成器
 * @param level
 * @author EinsiTang
 */
module.exports = (level = 0) => {
    // @TODO 生成数独题目
    if (true) {
        throw new Error(`生成器未实现，无法生存 level : ${level} 数独`)
    }

    return []
}